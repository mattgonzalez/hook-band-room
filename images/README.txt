Drop photos here:

  hero.jpg       — wide shot behind the masthead (stage/band photo).
                   ~1600px wide looks best; it's darkened automatically
                   so the type stays readable.
  boden-nx7.jpg  — thumbnail for the Backline card (square-ish crop).

Add more gear photos with any filename and reference them in
data/band.js. Missing files are skipped gracefully.
